version: '2'

services:
  rabbitmq:
    build: ./rabbitmq
    image: "rabbitmq-server"
    network_mode: "host"
    ports:
      - "15672:15672"

  clamavd:
    build: ./clamavd
    image: "clamav-daemon"
    network_mode: "host"
    ports:
      - "3310:3310"
