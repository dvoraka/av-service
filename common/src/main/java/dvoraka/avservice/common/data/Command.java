package dvoraka.avservice.common.data;

/**
 * Enum for command types.
 */
public enum Command {
    DISCOVER,
    EXISTS,
    LOCK,
    SEQUENCE,
    STATUS,

    SAVE,
    LOAD,
    UPDATE,
    DELETE
}
