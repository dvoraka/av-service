package dvoraka.avservice.common.data;

/**
 * Enum for command types.
 */
public enum Command {
    DISCOVER,
    EXISTS,
    LOCK,
    SEQUENCE,
    STATUS,
    UNLOCK,

    SAVE,
    LOAD,
    UPDATE,
    DELETE
}
